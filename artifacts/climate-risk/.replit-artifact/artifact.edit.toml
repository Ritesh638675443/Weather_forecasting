kind = "web"
previewPath = "/"
title = "Climate Risk Dashboard"
version = "1.0.0"

[[services]]
localPort = 5000
name = "Climate Risk Dashboard"
paths = ["/"]

[services.development]
run = "cd artifacts/climate-risk && streamlit run app.py --server.port 5000"
